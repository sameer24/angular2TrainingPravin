import { FirsLetterCaptialPipe } from './firs-letter-captial.pipe';

describe('FirsLetterCaptialPipe', () => {
  it('create an instance', () => {
    const pipe = new FirsLetterCaptialPipe();
    expect(pipe).toBeTruthy();
  });
});
